Scripts for training and evaluating cycle count estimation.

Train cycle count models:
```bash
python3 train.py
```

Evaluate them:

```bash
python3 test.py
```

Inspecting data layout:

```bash
python3 info.py
```